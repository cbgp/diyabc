DONE virer conditions dont le param n'existe plus : à chaque defpriors
DONE ameliorer la gestion de l'ecriture/lecture : 
DONE save -> sauve un fichier conf.tmp
DONE exit set hist -> sauve conf.hist.tmp
DONE exit set gen -> sauve conf.gen.tmp
DONE mettre le nombre de paramètres d'un scenarion entre parenthèses dans la première ligne du scenario dans le fichier de conf
DONE gerer clear genData
DONE ecrire les entetes (fin du reftable header)
DONE get nb param de genData
DONE gerer la verif des valeurs des deux setmutationmodel S et M
DONE gerer la validation du gentData : le passer à valide ssi tous les setmutmod des groupes sont valides et il exsite au moins une sum stat pr chaque groupe
DONE Verif save de shape ds setmutationModel Microstats
DONE système de documentation utilisateur
DONE installeur mac et windows

protocole de communication

importer analyses

mieux gerer lancements sur cluster : 
- proposer creation d'un tar (deja prevu)
- proposer creation job proactive (prefs)

