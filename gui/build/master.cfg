# -*- python -*-
# ex: set syntax=python:

# This is a sample buildmaster config file. It must be installed as
# 'master.cfg' in your buildmaster's base directory.

# This is the dictionary that the buildmaster pays attention to. We also use
# a shorter alias to save typing.
c = BuildmasterConfig = {}

####### BUILDSLAVES

# The 'slaves' list defines the set of recognized buildslaves. Each element is
# a BuildSlave object, specifying a unique slave name and password.  The same
# slave name and password must be configured on the slave.
from buildbot.buildslave import BuildSlave
c['slaves'] = [BuildSlave("windows7", "pass"),BuildSlave("macosx","pass"),BuildSlave("linux64","pass"),BuildSlave("linux32","pass")]

# 'slavePortnum' defines the TCP port to listen on for connections from slaves.
# This must match the value configured into the buildslaves (with their
# --master option)
c['slavePortnum'] = 9989

####### CHANGESOURCES

# the 'change_source' setting tells the buildmaster how it should find out
# about source code changes.  Here we point to the buildbot clone of pyflakes.

from buildbot.changes.gitpoller import GitPoller
c['change_source'] = []
c['change_source'].append(GitPoller(
        'https://github.com/cbgp/diyabc.git',
        workdir='diyabc', branch='master',
        pollinterval=300))

####### SCHEDULERS

# Configure the Schedulers, which decide how to react to incoming changes.  In this
# case, just kick off a 'runtests' build

from buildbot.schedulers.basic import SingleBranchScheduler
from buildbot.schedulers.forcesched import ForceScheduler
from buildbot.changes import filter
c['schedulers'] = []
#c['schedulers'].append(SingleBranchScheduler(
#                            name="all",
#                            change_filter=filter.ChangeFilter(branch='master'),
#                            treeStableTimer=None,
#                            builderNames=["buildlin64"]))
c['schedulers'].append(ForceScheduler(
                            name="forcelinrpm32",
                            builderNames=["buildlinrpm32"]))
c['schedulers'].append(ForceScheduler(
                            name="forcelinrpm64",
                            builderNames=["buildlinrpm64"]))
c['schedulers'].append(ForceScheduler(
                            name="forcelindeb32",
                            builderNames=["buildlindeb32"]))
c['schedulers'].append(ForceScheduler(
                            name="forcelindeb64",
                            builderNames=["buildlindeb64"]))
c['schedulers'].append(ForceScheduler(
                            name="forcelin32",
                            builderNames=["buildlin32"]))
c['schedulers'].append(ForceScheduler(
                            name="forcelin64",
                            builderNames=["buildlin64"]))
c['schedulers'].append(ForceScheduler(
                            name="forcemac",
                            builderNames=["buildmac32"]))
c['schedulers'].append(ForceScheduler(
                            name="forcewin",
                            builderNames=["buildwin32"]))

####### BUILDERS

# The 'builders' list defines the Builders, which tell Buildbot how to perform a build:
# what steps, and which slaves can execute them.  Note that any particular build will
# only take place on one slave.

from buildbot.process.factory import BuildFactory
from buildbot.steps.source import Git
from buildbot.steps.shell import ShellCommand
from buildbot.steps.transfer import FileUpload
from buildbot.steps.transfer import DirectoryUpload
from buildbot.steps.master import MasterShellCommand

factorylinrpm64 = BuildFactory()
factorylinrpm64.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
factorylinrpm64.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build/rpm/ ; ./rpmbuild_all.sh 64"]))
factorylinrpm64.addStep(DirectoryUpload(slavesrc="../source/gui/build/rpm/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorylinrpm64.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorylinrpm32 = BuildFactory()
factorylinrpm32.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
factorylinrpm32.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build/rpm/ ; ./rpmbuild-bin.sh 32 ; mkdir lastDiyabc ; mv *.rpm lastDiyabc/"]))
factorylinrpm32.addStep(DirectoryUpload(slavesrc="../source/gui/build/debian/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorylinrpm32.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorylindeb32 = BuildFactory()
factorylindeb32.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
factorylindeb32.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build/debian/ ; fakeroot ./build_deb_bin_32_only.sh ../../../Makefile ../../version.txt"]))
factorylindeb32.addStep(DirectoryUpload(slavesrc="../source/gui/build/debian/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorylindeb32.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorylindeb64 = BuildFactory()
factorylindeb64.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
factorylindeb64.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build/debian/ ; ./build_all.sh amd64"]))
factorylindeb64.addStep(DirectoryUpload(slavesrc="../source/gui/build/debian/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorylindeb64.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorylin32 = BuildFactory()
# check out the source
factorylin32.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
#factorylin.addStep(Git(repourl='/home/julien/vcs/git/diyabc.git', mode='copy'))
factorylin32.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build ; ./package-lin-x32_builder.sh"]))
#factorylin.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/python_interface/docs/project_builders ; pwd"]))
#factorylin.addStep(FileUpload(slavesrc="python_interface/docs/project_builders/diyabc.zip",masterdest="~/public_html/diylin64.zip"))
factorylin32.addStep(DirectoryUpload(slavesrc="../source/gui/build/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorylin32.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorylin64 = BuildFactory()
factorylin64.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
factorylin64.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build ; ./package-lin-x64_builder.sh"]))
factorylin64.addStep(DirectoryUpload(slavesrc="../source/gui/build/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorylin64.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorymac = BuildFactory()
# check out the source
factorymac.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
#factorymac.addStep(Git(repourl='/home/julien/vcs/git/diyabc.git', mode='copy'))
factorymac.addStep(ShellCommand(command=["/bin/sh","-c","cd ../source/gui/build ; ./package-mac-i386_builder.sh"]))
factorymac.addStep(DirectoryUpload(slavesrc="../source/gui/build/lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
#factorylin.addStep(FileUpload(slavesrc="python_interface/docs/project_builders/lastDiyabc/diyabc-2.0.dmg",masterdest="~/public_html/diy.dmg"))
factorymac.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

factorywin = BuildFactory()
# check out the source
factorywin.addStep(Git(repourl='https://github.com/cbgp/diyabc.git', mode='copy'))
#factorywin.addStep(Git(repourl='/home/julien/vcs/git/diyabc.git', mode='copy'))

## !! il faut que le dossier git/bin soit dans le path de windows

factorywin.addStep(ShellCommand(command=["sh.exe","-c","cd ../source/gui/build ; ./package-win-i386_builder.sh"]))
#factorywin.addStep(ShellCommand(command=['sh.exe', "-c", "pwd"]))
#factorywin.addStep(ShellCommand(command=["dir"]))
#factorywin.addStep(ShellCommand(command=['cd', 'python_interface' ,'\n','dir']))
factorywin.addStep(DirectoryUpload(slavesrc="..\\source\\gui\\build\\lastDiyabc", masterdest="~/public_html/lastDiyabc",url="http://147.99.65.15/~julien/lastDiyabc/"))
factorywin.addStep(MasterShellCommand(command="""
chmod -R 755 ~/public_html/lastDiyabc
"""))

from buildbot.config import BuilderConfig

c['builders'] = []
c['builders'].append(
    BuilderConfig(name="buildlinrpm32",
      slavenames=["linux32"],
      factory=factorylinrpm32))
c['builders'].append(
    BuilderConfig(name="buildlinrpm64",
      slavenames=["linux64"],
      factory=factorylindeb64))
c['builders'].append(
    BuilderConfig(name="buildlindeb32",
      slavenames=["linux32"],
      factory=factorylindeb32))
c['builders'].append(
    BuilderConfig(name="buildlindeb64",
      slavenames=["linux64"],
      factory=factorylindeb64))
c['builders'].append(
    BuilderConfig(name="buildlin32",
      slavenames=["linux32"],
      factory=factorylin32))
c['builders'].append(
    BuilderConfig(name="buildlin64",
      slavenames=["linux64"],
      factory=factorylin64))
c['builders'].append(
    BuilderConfig(name="buildmac32",
      slavenames=["macosx"],
      factory=factorymac))
c['builders'].append(
    BuilderConfig(name="buildwin32",
      slavenames=["windows7"],
      factory=factorywin))

####### STATUS TARGETS

# 'status' is a list of Status Targets. The results of each build will be
# pushed to these targets. buildbot/status/*.py has a variety to choose from,
# including web pages, email senders, and IRC bots.

c['status'] = []

from buildbot.status import html
from buildbot.status.web import authz, auth

authz_cfg=authz.Authz(
    # change any of these to True to enable; see the manual for more
    # options
    auth=auth.BasicAuth([("diyabc","diyabc")]),
    gracefulShutdown = False,
    forceBuild = 'auth', # use this to test your slave once it is set up
    forceAllBuilds = True,
    pingBuilder = False,
    stopBuild = True,
    stopAllBuilds = True,
    cancelPendingBuild = True,
)
c['status'].append(html.WebStatus(http_port=8010, authz=authz_cfg))

####### PROJECT IDENTITY

# the 'title' string will appear at the top of this buildbot
# installation's html.WebStatus home page (linked to the
# 'titleURL') and is embedded in the title of the waterfall HTML page.

c['title'] = "DIYABC"
c['titleURL'] = "www1.montpellier.inra.fr/CBGP/diyabc/"

# the 'buildbotURL' string should point to the location where the buildbot's
# internal web server (usually the html.WebStatus page) is visible. This
# typically uses the port number set in the Waterfall 'status' entry, but
# with an externally-visible host name which the buildbot cannot figure out
# without some help.

c['buildbotURL'] = "http://147.99.65.15:8010/"

####### DB URL

c['db'] = {
    # This specifies what database buildbot uses to store its state.  You can leave
    # this at its default for all but the largest installations.
    'db_url' : "sqlite:///state.sqlite",
}
